declare namespace GlobalMixins
{
    interface Renderer
    {
        readonly prepare: import('pixijs/prepare').Prepare;
    }

    interface Settings
    {
        /** @deprecated since 7.1.0 */
        UPLOADS_PER_FRAME: number;
    }

    interface IRenderer
    {
        readonly prepare: import('pixijs/prepare').BasePrepare;
    }
}
